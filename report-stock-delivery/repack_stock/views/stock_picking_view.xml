<odoo>

    <record id="view_picking_form_repack_tree" model="ir.ui.view">
        <field name="name">stock.picking.form.repack.tree</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">

            <!-- Tambah Page Repack -->
            <xpath expr="//sheet/notebook" position="inside">

                <page string="Repack">

                    <!-- Button -->

                    <field name="is_repack_done" invisible="1"/>

                    <!-- ===================== -->
                    <!-- REPACK LINE (PRODUCT A) -->
                    <!-- ===================== -->

                    <field name="repack_line_ids">
                        <list>
                            <field name="product_a_id"/>
                            <field name="qty_a"/>
                            <field name="move_a_state" string="Status Move" widget="badge"/>
                        </list>

                        <!-- FORM ketika klik baris -->
                        <form string="Repack Line">

                            <group>
                                <field name="product_a_id"/>
                                <field name="qty_a"/>
                                <field name="move_a_id" readonly="1" invisible="not move_a_id"/>
                                <field name="move_a_state" readonly="1" invisible="not move_a_id" widget="badge"/>
                            </group>

                            <separator string="Hasil Repack"/>

                            <!-- ===================== -->
                            <!-- OUTPUT (PRODUCT B BANYAK) -->
                            <!-- ===================== -->

                            <field name="repack_output_ids">
                                <list editable="bottom">

                                    <field name="product_b_id"/>
                                    <field name="qty_b"/>
                                    <field name="move_b_state" string="Status Move" widget="badge"/>

                                </list>

                                <form string="Repack Output">
                                    <group>
                                        <field name="product_b_id"/>
                                        <field name="qty_b"/>
                                        <field name="move_b_id" readonly="1" invisible="not move_b_id"/>
                                        <field name="move_b_state" readonly="1" invisible="not move_b_id" widget="badge"/>
                                    </group>
                                </form>

                            </field>

                        </form>

                    </field>

                </page>

            </xpath>

        </field>
    </record>


</odoo>
