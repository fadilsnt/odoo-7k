<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="stock_picking_view_form_inherit_hd_inventory_custom" model="ir.ui.view">
        <field name="name">stock.picking.view.form.inherit.hd.inventory.custom</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='move_ids_without_package']//list//field[@name='product_uom_qty']" position="after">
                <field name="sales_person_ids" string="Pemilik" widget="many2many_tags" options="{'no_create': True}"/>
                <field name="owner_id" string="Sale Persons" options="{'no_create': True}"/>
            </xpath>

            <xpath expr="//field[@name='move_ids_without_package']//list//field[@name='product_uom']" position="after">
                <field name="tonase_asli"/>
                <field name="is_tonase_move_line" column_invisible="1"/>
            </xpath>            

            <xpath expr="//notebook" position="inside">
                <page name="laporan_harian" string="Laporan Harian">
                    <notebook>
                        <page name="product_line_ids" string="Product Lines">
                            <field name="move_line_ids" widget="one2many_search">
                                <list editable="bottom" create="0" limit="999">
                                    <field name="product_id"/>
                                    <field name="quantity" />
                                    <field name="product_uom_category_id" column_invisible="1"/>
                                    <field name="product_uom_id" readonly="0"/>
                                    <field name="tonase_asli" />
                                    <field name="oven_number" />
                                    <field name="production_date" />
                                    <field name="line_packing" />
                                    <field name="camp_tgl_briket" />
                                    <field name="briket_tgu" />
                                    <field name="shift_briket" />
                                    <field name="pembakar_penutup" />
                                    <field name="asumsi_berat_ikat" />
                                    <field name="lubang_setom" />
                                    <field name="bongkaran" />
                                </list>
                            </field>
                        </page>
                        <page name="consume_move_ids" string="Consume Lines" invisible="picking_type_code != 'incoming'">
                            <field name="consume_move_ids" widget="one2many_search">
                                <list editable="bottom" create="0" limit="999">
                                    <field name="name"/>
                                    <field name="product_id"/>
                                    <field name="product_uom" optional="show"/>
                                    <field name="product_uom_qty"/>
                                    <field name="location_id" optional="hide"/>
                                    <field name="location_dest_id" optional="hide"/>
                                    <field name="is_consume" optional="hide"/>
                                    <field name="state" optional="hide"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </page>
            </xpath>

            <xpath expr="//field[@name='owner_id']" position="after">
                <field name="pemilik_ids" widget="many2many_tags" readonly="1" invisible="1"/>
            </xpath>

            <xpath expr="//field[@name='backorder_id']" position="before">
                <button name="%(action_wizard_buat_laporan_harian_picking)d"
                        string="BUAT LAPORAN HARIAN"
                        type="action"
                        context="{
                            'default_picking_id': id,
                            'default_location_dest_id': location_dest_id
                        }"
                        class="btn-primary" 
                        invisible="state == 'done'"/>                
            </xpath>      

            <xpath expr="//field[@name='origin']" position="after">
                <field name="btb_number" readonly="state in ['cancel', 'done']"/>
                <field name="partner_ref" readonly="1" invisible="not partner_ref"/>
            </xpath>
        </field>
    </record>   

    <record id="view_picking_form_inherit_sales_person_invisible" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit.sales.person.invisible</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="export_stock_report.view_picking_form_inherit_sales_person"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='person_ids']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>

    <record id="stock_picking_form_inherit_owner_invisible" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit.owner.invisible</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='owner_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>   

    <record id="vpicktree_custom_view_only" model="ir.ui.view">
        <field name="name">stock.picking.list</field>
        <field name="model">stock.picking</field>
        <field name="mode">primary</field>
        <field name="priority">99</field>
        <field name="groups_id" eval="[(6,0,[ref('hd_inventory_custom.group_picking_view_only')])]"/>
        <field name="arch" type="xml">
            <list string="Picking list" multi_edit="1" sample="1" js_class="stock_list_view" create="0" edit="0" delete="0">
                <header>
                    <button name="do_unreserve" type="object" string="Unreserve"/>
                    <button name="action_assign" type="object" string="Check Availability"/>
                </header>
                <field name="company_id" column_invisible="True"/>
                <field name="priority" optional="show" widget="priority" nolabel="1"/>
                <field name="name" decoration-bf="1"/>
                <field name="location_id" options="{'no_create': True}" string="From" groups="stock.group_stock_multi_locations" optional="show" readonly="state == 'done'"/>
                <field name="location_dest_id" options="{'no_create': True}" string="To" groups="stock.group_stock_multi_locations" optional="show" readonly="state == 'done'"/>
                <field name="partner_id" optional="show" readonly="state in ['cancel', 'done']"/>
                <field name="is_signed" string="Signed" optional="hide" groups="stock.group_stock_sign_delivery"/>
                <field name="user_id" optional="hide" widget="many2one_avatar_user" readonly="state in ['cancel', 'done']"/>
                <field name="scheduled_date" optional="show" widget="remaining_days" invisible="state in ('done', 'cancel')" readonly="state in ['cancel', 'done']"/>
                <field name="picking_type_code" column_invisible="True"/>
                <field name="products_availability_state" column_invisible="True" options='{"lazy": true}'/>
                <field name="products_availability" options='{"lazy": true}'
                    optional="hide"
                    invisible="picking_type_code != 'outgoing' or state not in ['confirmed', 'waiting', 'assigned']"
                    decoration-success="state == 'assigned' or products_availability_state == 'available'"
                    decoration-warning="state != 'assigned' and products_availability_state in ('expected', 'available')"
                    decoration-danger="state != 'assigned' and products_availability_state == 'late'"/>
                <field name="date_deadline" optional="hide" widget="remaining_days" invisible="state in ('done', 'cancel')"/>
                <field name="date_done" string="Effective Date" optional="hide"/>
                <field name="origin" optional="show" readonly="state in ['cancel', 'done']"/>
                <field name="backorder_id" optional="hide"/>
                <field name="picking_type_id" optional="hide"/>
                <field name="company_id" groups="base.group_multi_company" optional="show"/>
                <field name="state" optional="show" widget="badge"
                        decoration-danger="state=='cancel'"
                        decoration-info="state== 'assigned'"
                        decoration-muted="state == 'draft'"
                        decoration-success="state == 'done'"
                        decoration-warning="state not in ('draft','cancel','done','assigned')"/>
                <field name="activity_exception_decoration" widget="activity_exception"/>
                <field name="json_popover" widget="stock_rescheduling_popover" nolabel="1" invisible="not json_popover"/>
            </list>
        </field>
    </record>    

    <record id="view_picking_form_view_only" model="ir.ui.view">
        <field name="name">stock.picking.form</field>
        <field name="model">stock.picking</field>
        <field name="mode">primary</field>
        <field name="priority">99</field>  
        <field name="groups_id" eval="[(6,0,[ref('hd_inventory_custom.group_picking_view_only')])]"/>      
        <field name="arch" type="xml">
            <form string="Transfer" create="false" edit="false" delete="false">
                <header>
                    <field name="state" widget="statusbar" invisible="picking_type_code != 'incoming'" statusbar_visible="draft,assigned,done"/>
                    <field name="state" widget="statusbar" invisible="picking_type_code == 'incoming'" statusbar_visible="draft,confirmed,assigned,done"/>
                </header>
                <sheet readonly="1">
                    <div class="oe_title">
                        <h1 class="d-flex">
                            <field name="priority" widget="priority" class="me-3"/>
                            <div invisible="id">New <field name="picking_type_code"/></div>
                            <field name="name" invisible="not id"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <div class="o_td_label">
                                <label for="partner_id" string="Delivery Address" style="font-weight:bold;"
                                        invisible="picking_type_code != 'outgoing'"/>
                                <label for="partner_id" string="Receive From" style="font-weight:bold;"
                                        invisible="picking_type_code != 'incoming'"/>
                                <label for="partner_id" string="Contact" style="font-weight:bold;"
                                        invisible="picking_type_code in ['incoming', 'outgoing']"/>
                            </div>
                            <field name="partner_id" nolabel="1" readonly="state in ['cancel', 'done']" options="{'no_open': True, 'no_create': True, 'no_create_edit': True}" placeholder="e.g. Lumber Inc"/>
                            <field name="picking_type_id" options="{'no_open': True}"
                                    readonly="state in ('done', 'cancel')"
                                    domain="[('code', 'in', ('incoming', 'outgoing', 'internal'))]" />
                            <field name="location_id" groups="!stock.group_stock_multi_locations" invisible="1" readonly="state == 'done'" options="{'no_open': True, 'no_create': True, 'no_create_edit': True}"/>
                            <field name="location_dest_id" groups="!stock.group_stock_multi_locations" invisible="1" readonly="state == 'done'" options="{'no_open': True, 'no_create': True, 'no_create_edit': True}"/>
                            <field name="location_id" groups="stock.group_stock_multi_locations" invisible="picking_type_code == 'incoming'" readonly="state == 'done'" options="{'no_open': True, 'no_create': True, 'no_create_edit': True}"/>
                            <field name="location_dest_id" groups="stock.group_stock_multi_locations" invisible="picking_type_code == 'outgoing'" readonly="state == 'done'" options="{'no_open': True, 'no_create': True, 'no_create_edit': True}"/>
                            <field name="backorder_id" invisible="not backorder_id" options="{'no_open': True, 'no_create': True, 'no_create_edit': True}"/>
                            <field name="use_create_lots" invisible="1"/> <!-- used by embedded stock.move list view below -->
                        </group>
                        <group>
                            <label for="scheduled_date"/>
                            <div class="o_row">
                                <field name="scheduled_date" readonly="state in ['cancel', 'done']" required="id"
                                    decoration-warning="state not in ('done', 'cancel') and scheduled_date &lt; now"
                                    decoration-danger="state not in ('done', 'cancel') and scheduled_date &lt; current_date"
                                    decoration-bf="state not in ('done', 'cancel') and (scheduled_date &lt; current_date or scheduled_date &lt; now)"/>
                                <field name="json_popover" nolabel="1" widget="stock_rescheduling_popover" invisible="not json_popover"/>
                            </div>
                            <field name="date_deadline"
                                invisible="state in ('done', 'cancel') or not date_deadline"
                                decoration-danger="date_deadline and date_deadline &lt; current_date"
                                decoration-bf="date_deadline and date_deadline &lt; current_date"/>
                            <field name="products_availability_state" invisible="1"/>
                            <field name="products_availability"
                                invisible="picking_type_code != 'outgoing' or state not in ['confirmed', 'waiting', 'assigned']"
                                decoration-success="state == 'assigned' or products_availability_state == 'available'"
                                decoration-warning="state != 'assigned' and products_availability_state in ('expected', 'available')"
                                decoration-danger="state != 'assigned' and products_availability_state == 'late'"/>
                            <field name="date_done" string="Effective Date" invisible="state != 'done'"/>
                            <field name="origin" placeholder="e.g. PO0032" readonly="state in ['cancel', 'done']"/>
                            <field name="owner_id" groups="stock.group_tracking_owner"
                                    invisible="picking_type_code != 'incoming'"
                                    readonly="state in ['cancel', 'done']"/>
                        </group>
                    </group>
                    <field name="picking_properties" columns="2"/>
                    <notebook>
                        <page string="Operations" name="operations">
                            <field name="move_ids_without_package" mode="list,kanban"
                                widget="stock_move_one2many"
                                readonly="state == 'done' and is_locked"
                                context="{'default_company_id': company_id, 'default_date': scheduled_date, 'default_date_deadline': date_deadline, 'picking_type_code': picking_type_code, 'default_picking_id': id, 'form_view_ref': 'stock.view_stock_move_operations', 'address_in_id': partner_id, 'default_picking_type_id': picking_type_id, 'default_location_id': location_id, 'default_location_dest_id': location_dest_id, 'default_partner_id': partner_id}"
                                add-label="Add a Product">
                                <list decoration-muted="scrapped == True or state == 'cancel' or (state == 'done' and is_locked == True)" string="Stock Moves" create="0" edit="0" delete="0">
                                    <field name="company_id" column_invisible="True"/>
                                    <field name="picking_id" column_invisible="True"/>
                                    <field name="name" column_invisible="True"/>
                                    <field name="state" readonly="0" column_invisible="True"/>
                                    <field name="picking_type_id" column_invisible="True"/>
                                    <field name="move_line_ids" column_invisible="True"/>
                                    <field name="location_id" column_invisible="True"/>
                                    <field name="location_dest_id" column_invisible="True"/>
                                    <field name="partner_id" column_invisible="True" readonly="state == 'done'"/>
                                    <field name="scrapped" column_invisible="True"/>
                                    <field name="picking_code" column_invisible="True"/>
                                    <field name="show_details_visible" column_invisible="True"/>
                                    <field name="additional" column_invisible="True"/>
                                    <field name="move_lines_count" column_invisible="True"/>
                                    <field name="is_locked" column_invisible="True"/>
                                    <field name="product_uom_category_id" column_invisible="True"/>
                                    <field name="is_storable" column_invisible="True"/>
                                    <field name="has_tracking" column_invisible="True"/>
                                    <field name="product_id" context="{'default_is_storable': True}" required="1" options="{'no_open': True, 'no_create': True, 'no_create_edit': True}" readonly="(state != 'draft' and not additional) or move_lines_count &gt; 0" force_save="1"/>
                                    <field name="location_final_id" optional="hide" options="{'no_open': True, 'no_create': True, 'no_create_edit': True}" groups="stock.group_stock_multi_locations"/>
                                    <field name="description_picking" string="Description" optional="hide"/>
                                    <field name="date" optional="hide"/>
                                    <field name="date_deadline" optional="hide"/>
                                    <field name="is_quantity_done_editable" column_invisible="True"/>
                                    <field name="show_quant" column_invisible="True"/>
                                    <field name="show_lots_text" column_invisible="True"/>
                                    <field name="show_lots_m2o" column_invisible="True"/>
                                    <field name="is_initial_demand_editable" column_invisible="True"/>
                                    <field name="display_import_lot" column_invisible="True"/>
                                    <!-- <field name="picking_type_entire_packs" column_invisible="True"/> -->
                                    <field name="product_packaging_id" groups="product.group_stock_packaging"
                                        context="{'default_product_id': product_id}"
                                        readonly="not product_id" options="{'no_open': True, 'no_create': True, 'no_create_edit': True}"/>
                                    <field name="product_uom_qty" string="Demand" readonly="not is_initial_demand_editable" options="{'no_open': True, 'no_create': True, 'no_create_edit': True}"/>
                                    <button type="object" name="action_product_forecast_report" title="Forecast Report" icon="fa-area-chart"
                                        invisible="not product_id or product_uom_qty == 0 or quantity == 0 and forecast_availability &lt;= 0 or (parent.picking_type_code == 'outgoing' and state != 'draft')"/>
                                    <button type="object" name="action_product_forecast_report" title="Forecast Report" icon="fa-area-chart text-danger"
                                        invisible="not product_id or product_uom_qty == 0 or quantity &gt; 0 or forecast_availability &gt; 0 or (parent.picking_type_code == 'outgoing' and state != 'draft')"/>
                                    <field name="forecast_expected_date" column_invisible="True"/>
                                    <field name="forecast_availability" string="Forecast" optional="hide"
                                        column_invisible="parent.state in ('draft', 'done') or parent.picking_type_code != 'outgoing'" widget="forecast_widget"/>
                                    <field name="product_qty" readonly="1" column_invisible="True"/>
                                    <field name="quantity" string="Quantity" readonly="not is_quantity_done_editable" column_invisible="parent.state=='draft'" decoration-danger="product_uom_qty and quantity > product_uom_qty and parent.state not in ['done', 'cancel']"/>
                                    <field name="product_uom" readonly="state != 'draft' and not additional" options="{'no_open': True, 'no_create': True}" string="Unit" groups="uom.group_uom"/>
                                    <field name="product_uom" groups="!uom.group_uom" column_invisible="True"/>
                                    <field name="picked" optional="hide" column_invisible="parent.state=='draft'"/>
                                    <field name="lot_ids" widget="many2many_tags"
                                        readonly="has_tracking != 'serial'"
                                        column_invisible="parent.state == 'draft'"
                                        groups="stock.group_production_lot"
                                        invisible="not show_details_visible or has_tracking == 'none'"
                                        optional="hide"
                                        options="{'create': [('parent.use_create_lots', '=', True)]}"
                                        context="{'default_company_id': company_id, 'default_product_id': product_id, 'active_picking_id': parent.id}"
                                        domain="[('product_id','=',product_id)]"
                                    />
                                </list>
                            </field>
                            <field name="id" invisible="1"/>
                            <!-- <field name="package_level_ids" context="{'default_location_id': location_id, 'default_location_dest_id': location_dest_id, 'default_company_id': company_id}" invisible="not picking_type_entire_packs" readonly="state == 'done'" /> -->
                            <button class="btn-secondary float-end" name="action_put_in_pack" type="object" string="Put in Pack" invisible="state in ('draft', 'done', 'cancel')" groups="stock.group_tracking_lot" data-hotkey="shift+g"/>
                        </page>
                        <page string="Additional Info" name="extra">
                            <group>
                                <group string="Other Information" name="other_infos">
                                    <field name="picking_type_code" invisible="1"/>
                                    <field name="move_type" invisible="picking_type_code == 'incoming'" readonly="state in ['cancel', 'done']"/>
                                    <field name="user_id" widget="many2one_avatar_user" domain="[('share', '=', False)]" readonly="state in ['cancel', 'done']" options="{'no_open': True, 'no_create': True, 'no_create_edit': True}"/>
                                    <field name="group_id" groups="base.group_no_one" options="{'no_open': True, 'no_create': True, 'no_create_edit': True}"/>
                                    <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" force_save="1"/>
                                </group>
                            </group>
                        </page>
                        <page string="Note" name="note">
                            <field name="note" string="Note" placeholder="Add an internal note that will be printed on the Picking Operations sheet"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Start Sparepart -->
    <record id="view_stock_picking_sparepart_usage_tree" model="ir.ui.view">
        <field name="name">stock.picking.sparepart.usage.tree</field>
        <field name="model">stock.picking</field>
        <field name="arch" type="xml">
            <list string="Sparepart Usage" decoration-success="state == 'done'" decoration-warning="state == 'assigned'" decoration-info="state == 'confirmed'">
                <field name="name"/>
                <field name="scheduled_date"/>
                <field name="origin"/>
                <field name="location_id"/>
                <field name="location_dest_id"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <record id="view_stock_picking_sparepart_usage_form" model="ir.ui.view">
        <field name="name">stock.picking.sparepart.usage.form</field>
        <field name="model">stock.picking</field>
        <field name="arch" type="xml">
            <form string="Sparepart Usage">
                <header>
                    <button name="action_confirm" invisible="state != 'draft'" string="Mark as Todo" type="object" class="oe_highlight" groups="base.group_user" data-hotkey="q"/>
                    <button name="action_assign" invisible="not show_check_availability" string="Check Availability" type="object" class="oe_highlight" groups="base.group_user" data-hotkey="w"/>                
                    <button name="button_validate" string="Validate" type="object" class="btn-success" invisible="state not in ('assigned','confirmed')"/>
                    <button name="action_cancel" invisible="state not in ('assigned', 'confirmed', 'draft', 'waiting')" string="Cancel" groups="base.group_user" type="object" confirm="Are you sure you want to cancel this transfer?" data-hotkey="x"/>
                    <button name="action_return_sparepart" string="Return Sparepart" type="object" class="btn-secondary" invisible="state != 'done'"/>                    
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,assigned,done"/>
                </header>
                <sheet>
                    <widget name="web_ribbon" title="Done" bg_color="text-bg-success" invisible="state != 'done'"/>                
                    <div class="oe_button_box" name="button_box">
                        <button name="action_detailed_operations" class="oe_stat_button" icon="fa-bars" type="object" help="List view of detailed operations">
                            <div class="o_form_field o_stat_info">
                                <span class="o_stat_text">Moves</span>
                            </div>
                        </button>
                        <button name="action_view_return_pickings" type="object" class="oe_stat_button" icon="fa-undo">
                            <field name="return_count" widget="statinfo" string="Returns"/>
                        </button>                        
                    </div>                
                    <div class="oe_title">
                        <h1 class="d-flex">
                            <div invisible="id">New <field name="picking_type_code"/></div>
                            <field name="name" invisible="not id" readonly="1"/>
                        </h1>
                    </div>  
                    <group string="Operation Information">
                        <group>
                            <field name="picking_type_id" domain="[('default_location_dest_id', '=', %(hd_inventory_custom.stock_location_sparepart_usage)d)]" readonly="state == 'done'"/>
                        </group>

                        <group>
                            <field name="date_done" invisible="state != 'done'"/>
                            <field name="products_availability_state" invisible="1"/>
                            <field name="products_availability"
                                invisible="picking_type_code != 'outgoing' or state not in ['confirmed', 'waiting', 'assigned']"
                                decoration-success="state == 'assigned' or products_availability_state == 'available'"
                                decoration-warning="state != 'assigned' and products_availability_state in ('expected', 'available')"
                                decoration-danger="state != 'assigned' and products_availability_state == 'late'"/>
                            <field name="date_done" string="Effective Date" invisible="state != 'done'"/>                            
                            <field name="is_sparepart_usage" invisible="1"/>                            
                        </group>
                    </group>

                    <group string="Request Information">
                        <group>
                            <field name="requestor_id" readonly="state in ['confirmed', 'waiting', 'assigned', 'done']" required="is_sparepart_usage"/>
                            <field name="scheduled_date" readonly="state in ['confirmed', 'waiting', 'assigned', 'done']"/>
                            <field name="sparepart_usage_attachment" invisible="not is_sparepart_usage" widget="many2many_binary" readonly="state in ['confirmed', 'waiting', 'assigned', 'done']"/>
                        </group>
                        <group>
                            <field name="location_id" readonly="1"/>
                            <field name="location_dest_id" readonly="1"/>
                        </group>
                    </group>                                  
                    <notebook>
                        <page string="Sparepart Details">
                            <field name="move_ids_without_package" 
                                mode="list"
                                widget="stock_move_one2many"
                                readonly="state == 'done' and is_locked"
                                context="{
                                        'default_picking_id': id,
                                        'default_picking_type_id': picking_type_id,
                                        'default_location_id': location_id,
                                        'default_location_dest_id': location_dest_id,
                                        'default_company_id': company_id
                                }">
                                <list editable="bottom">
                                    <field name="company_id" column_invisible="1"/>
                                    <field name="picking_id" column_invisible="1"/>
                                    <field name="name" column_invisible="1"/>
                                    <field name="state" column_invisible="1"/>
                                    <field name="location_id" column_invisible="1"/>
                                    <field name="location_dest_id" column_invisible="1"/>
                                    <field name="product_uom_category_id" column_invisible="1"/>                                
                                    <field name="sparepart_category_id" column_invisible="1"/>

                                    <field name="product_id" context="{'default_is_storable': True}" options='{"no_open": True,"no_create": 1, "no_create_edit": 1}' domain="[('categ_id', 'child_of', sparepart_category_id)]" force_save="1"/>
                                    <field name="product_uom_qty" string="Demand" readonly="not is_initial_demand_editable"/>
                                    <button type="object" name="action_product_forecast_report" title="Forecast Report" icon="fa-area-chart"
                                        invisible="not product_id or product_uom_qty == 0 or quantity == 0 and forecast_availability &lt;= 0 or (parent.picking_type_code == 'outgoing' and state != 'draft')"/>
                                    <button type="object" name="action_product_forecast_report" title="Forecast Report" icon="fa-area-chart text-danger"
                                        invisible="not product_id or product_uom_qty == 0 or quantity &gt; 0 or forecast_availability &gt; 0 or (parent.picking_type_code == 'outgoing' and state != 'draft')"/>
                                    <field name="forecast_expected_date" column_invisible="True"/>
                                    <field name="forecast_availability" string="Forecast" optional="hide"
                                        column_invisible="parent.state in ('draft', 'done') or parent.picking_type_code != 'outgoing'" widget="forecast_widget"/>
                                    <field name="product_qty" readonly="1" column_invisible="True"/>
                                    <field name="quantity" string="Quantity" readonly="not is_quantity_done_editable" column_invisible="parent.state=='draft'" decoration-danger="product_uom_qty and quantity > product_uom_qty and parent.state not in ['done', 'cancel']"/>
                                    <field name="product_uom" string="UoM"/>
                                </list>
                            </field>
                        </page>
                        <page string="Note" name="note">
                            <field name="note" string="Note" placeholder="Add an internal note that will be printed on the Picking Operations sheet"/>
                        </page>                        
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>    

    <record id="view_stock_picking_sparepart_usage_search" model="ir.ui.view">
        <field name="name">stock.picking.sparepart.usage.search</field>
        <field name="model">stock.picking</field>
        <field name="arch" type="xml">
            <search string="Search Sparepart Usage">
                <field name="name"/>
                <field name="origin"/>
                <field name="state"/>
                <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
                <filter string="Waiting" name="waiting" domain="[('state','=','confirmed')]"/>
                <filter string="Ready" name="ready" domain="[('state','=','assigned')]"/>
                <filter string="Done" name="done" domain="[('state','=','done')]"/>
                <group expand="0" string="Group By">
                    <filter string="Status" name="group_state" context="{'group_by':'state'}"/>
                    <filter string="Scheduled Date" name="group_date" context="{'group_by':'scheduled_date'}"/>
                </group>
            </search>
        </field>
    </record>    

    <record id="action_stock_picking_sparepart_usage" model="ir.actions.act_window">
        <field name="name">Sparepart Usage</field>
        <field name="res_model">stock.picking</field>
        <field name="view_mode">list,form</field>
        <field name="view_ids"
            eval="[(5, 0, 0),
                (0, 0, {'view_mode': 'list','view_id': ref('hd_inventory_custom.view_stock_picking_sparepart_usage_tree')}),
                (0, 0, {'view_mode': 'form','view_id': ref('hd_inventory_custom.view_stock_picking_sparepart_usage_form')})
            ]"/>        
        <field name="search_view_id" ref="hd_inventory_custom.view_stock_picking_sparepart_usage_search"/>
        <field name="domain" eval="[('picking_type_id', '=', ref('hd_inventory_custom.picking_type_sparepart_usage'))]"/>
        <field name="context"
            eval="{
                'default_picking_type_id': ref('hd_inventory_custom.picking_type_sparepart_usage'),
                'default_location_id': ref('stock.stock_location_stock'),
                'default_location_dest_id': ref('hd_inventory_custom.stock_location_sparepart_usage'),
            }"/>
    </record>

    <menuitem id="menu_sparepart_request" name="Usage Request" parent="stock.menu_stock_transfers" action="action_stock_picking_sparepart_usage" sequence="25"/>           
    <!-- End Sparepart -->

    <!-- Incoming / Receipts -->
    <record id="action_picking_view_only_incoming" model="ir.actions.act_window">
        <field name="name">Penerimaan</field>
        <field name="res_model">stock.picking</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('picking_type_code','=','incoming')]</field>
        <field name="views" eval="[
            (ref('hd_inventory_custom.vpicktree_custom_view_only'), 'list'),
        ]"/>
        <field name="context">
            {
                'create': False,
                'edit': False,
                'delete': False
            }
        </field>        
    </record>

    <menuitem id="menu_receipt_view_only"
              name="Penerimaan"
              parent="stock.menu_stock_transfers"
              action="action_picking_view_only_incoming"
              groups="hd_inventory_custom.group_picking_view_only"
              sequence="20"/>

    <!-- Outgoing / Deliveries -->
    <record id="action_picking_view_only_outgoing" model="ir.actions.act_window">
        <field name="name">Pengiriman</field>
        <field name="res_model">stock.picking</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('picking_type_code','=','outgoing')]</field>
        <field name="views" eval="[
            (ref('hd_inventory_custom.vpicktree_custom_view_only'), 'list'),
        ]"/>
        <field name="context">
            {
                'create': False,
                'edit': False,
                'delete': False
            }
        </field>        
    </record>

    <menuitem id="menu_delivery_view_only"
              name="Pengiriman"
              parent="stock.menu_stock_transfers"
              action="action_picking_view_only_outgoing"
              groups="hd_inventory_custom.group_picking_view_only"
              sequence="21"/>

    <!-- Internal Transfer -->
    <record id="action_picking_view_only_internal" model="ir.actions.act_window">
        <field name="name">Transfer Internal</field>
        <field name="res_model">stock.picking</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('picking_type_code','=','internal')]</field>
        <field name="views" eval="[
            (ref('hd_inventory_custom.vpicktree_custom_view_only'), 'list'), 
        ]"/>
        <field name="context">
            {
                'create': False,
                'edit': False,
                'delete': False
            }
        </field>          
    </record>

    <menuitem id="menu_internal_view_only"
              name="Transfer Internal"
              parent="stock.menu_stock_transfers"
              action="action_picking_view_only_internal"
              groups="hd_inventory_custom.group_picking_view_only"
              sequence="22"/>
</odoo>